@using BullardEncuestas.Helpers
@using BullardEncuestas.Core.DTO
@using PagedList;
@model IPagedList<EncuestaDTO>

@{
    ViewBag.Title = "Encuestas";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<section class="hbox stretch">
    <aside>
        <section class="vbox">
            <section class="scrollable padder">
                <div class="panel-body row m-b">
                    <div class="col-xs-12 col-md-12">
                        <section class="panel panel-default">
                            <div class="row wrapper">
                                <div class="col-sm-8 m-b-xs">
                                    <h2>Encuestas</h2>
                                </div>
                                <div class="col-sm-4">
                                    <a class="btn btn-sm btn-success pull-right btn-rounded"><i class="fa fa-plus"></i> Crear nueva Encuesta</a>
                                </div>
                            </div>
                            <div class="table-responsive">
                                <table class="table table-striped b-t b-light" id="tableEncuestas">
                                    <thead>
                                        <tr>
                                            <th width="15%">Periodo</th>
                                            <th width="35%">Encuesta</th>
                                            <th width="25%">Resultados</th>
                                            <th width="25%">Opciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model != null)
                                        {
                                            foreach(var obj in Model)
                                            {
                                                <tr>
                                                    <td>@obj.Nombre</td>
                                                    <td>@obj.Periodo.Descripcion</td>
                                                    <td align="center">
                                                        <a href="@Url.Action("Encuesta", new { id = obj.IdEncuesta })"><i class="fa edit"></i></a>
                                                        <a href="@Url.Action("Encuesta", new { id = obj.IdEncuesta })"><i class="fa send"></i></a>
                                                        @*<a href="#@obj.IdTarea" class="AddTaskToBagTasks"><i class="fa fa-plus"></i></a>*@
                                                    </td>
                                                </tr>
                                            }
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </section>
                    </div>
                </div>
            </section>
        </section>
    </aside>
</section>
